After completing work, run the relevant verification for mobile/: lsp diagnostics or typecheck, npm run lint, npm test (often with --runInBand for deterministic output), and optionally npx expo export --platform ios --output-dir dist-export for build validation. For UI/runtime changes, verify the active iOS simulator session logs and note whether behavior is native AI mode or rule-based fallback mode.